services:
  telegram-interactive-bot:
    image: ghcr.io/acmuhan/telegram-interactive-bot:latest
    container_name: telegram-interactive-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
